# v14 fixtures — placeholder

Empty until fixtures are re-exported from a Foundry **v14** + PF2e world.
See `../README.md` for the export commands.

When `actor-pc.json`, `combat.json`, and `token.json` are added here, the
contract suite (`pf2eAdapter.test.js`) automatically runs the same assertions
against them. Any failure points at a path the adapter must branch on for v14.
Delete this file once the real fixtures land.
